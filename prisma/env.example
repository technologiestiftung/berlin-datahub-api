# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#using-environment-variables

# Prisma supports the native connection string format for PostgreSQL, MySQL and SQLite.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/testing?schema=public"

# we share this env with prisma
# but we need to explicitly call this env in our npm run dev script
# since the import of these envs is only done for prisma internals and the cli
NODE_ENV=development
APP_SECRET=superdupersecret
